{
  "name": "rivendell-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Snug web client (vanilla ES modules, no dependencies)",
  "scripts": {
    "test": "node --test test/*.test.js"
  }
}
