{
  "name": "rivendell-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Snug web client (vanilla ES modules, no runtime dependencies; Playwright is dev-only e2e tooling)",
  "scripts": {
    "test": "node --test test/*.test.js",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.53.0"
  }
}
