# Build artifacts
/bin/
snug

# Environment
.env
.env.local

# Node (used only as the frontend test runner — no deps, no package manifest)
node_modules/
package.json
package-lock.json

# OS / editor cruft
.DS_Store
*.swp
