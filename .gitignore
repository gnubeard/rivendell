# Build artifacts
/bin/
rivendell

# Environment
.env
.env.local

# sensitive credentials
.creds

# Node (used only as the frontend test runner — no deps, no package manifest)
node_modules/
package.json
package-lock.json

# OS / editor cruft
.DS_Store
*.swp
